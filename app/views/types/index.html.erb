<ul>
<% @types.each do |type| %>
  <li>
  <%= type.id %>. <%= type.name %>
  <%= link_to 'edit', edit_type_path(type) %>
  <%= delete_link type, "Delete type #{type.name}" %>
<% end %>
</ul>

<%= link_to "Create new type", new_type_path, class: 'button' %>
